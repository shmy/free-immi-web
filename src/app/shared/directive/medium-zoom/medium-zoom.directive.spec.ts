import { MediumZoomDirective } from './medium-zoom.directive';

describe('MediumZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new MediumZoomDirective();
    expect(directive).toBeTruthy();
  });
});
