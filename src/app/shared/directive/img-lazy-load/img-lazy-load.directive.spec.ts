import { ImgLazyLoadDirective } from './img-lazy-load.directive';

describe('ImgLazyLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgLazyLoadDirective();
    expect(directive).toBeTruthy();
  });
});
